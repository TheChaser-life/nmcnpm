# Model Promotion Lambda — Python dependencies
#
# boto3 is pre-installed in the AWS Lambda Python 3.11 runtime.
# Listed here for local development and testing purposes.
#
# Install for local dev:
#   pip install -r requirements.txt
#
# Install for testing:
#   pip install -r requirements.txt pytest

boto3==1.34.69
botocore==1.34.69
