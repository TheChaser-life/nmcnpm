-- Migration V0: Tạo database
-- Chạy lần đầu tiên với user postgres kết nối vào database mặc định (postgres)
-- Các migration V1-V4 phải được chạy SAU khi database này đã tồn tại

CREATE DATABASE currency_exchange;
